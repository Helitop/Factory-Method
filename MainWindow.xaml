<Window x:Class="Factory_Method.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Паттерны: Фабрики" Height="400" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0">
            <TextBlock Text="Выберите цвет: " VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="ColorSelector" Width="150" SelectionChanged="ColorSelector_SelectionChanged">
                <ComboBoxItem Content="Красный" Tag="Red"/>
                <ComboBoxItem Content="Синий" Tag="Blue"/>
                <ComboBoxItem Content="Зеленый" Tag="Green"/>
            </ComboBox>
        </StackPanel>

        <!-- Место, где будут рисоваться фигуры -->
        <StackPanel x:Name="DrawingArea" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Window>